+ setting up the overall structure with working CI for the new repository
+ update licence to Apache 2.0
+ add test function to ensure CI passes
+ add requirement files (created by running uv run pre-commit install)
